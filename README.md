## Password Generator
A React app that generates a 6-digit One-Time Password (OTP) with a 5-seconds expiration timer.

### Features
  - Random 6-digits OTP generation
  - Countdown timer using React hooks
  - Disabled button while OTP is active
  - Expiration message when timer reaches zero

Built with React (`useEffect`, `useState`)
  